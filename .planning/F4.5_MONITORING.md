# Plan de Monitoreo F4.5 — Bot-TiendaLasMotos Qwen Beta

## Contexto
- **Ticket:** BOT-BUILD-FLIP-QWEN-086
- **Estado:** flip a Qwen primario ejecutado en beta (`llm_runtime/global.qwen_enabled=true`).
- **Ventana de observación:** 24-48h antes de autorizar tráfico a prod.
- **Fuentes de datos:**
  - Cloud Logging: `resource.type=cloud_run_revision AND resource.labels.service_name=bot-tiendalasmotos-beta`
  - Langfuse: trazas `whatsapp_webhook_background` (proyecto `cmp0kl38703jvad07c1yq5xum`).
  - Firestore: doc `llm_runtime/global` (flag de ruta).

## Umbrales de alerta (F4.5)
| Métrica | Umbral ROJO | Umbral AMARILLO | Acción |
|---------|-------------|-----------------|--------|
| Tasa de errores 5xx / timeout en `/webhook` | > 1% | 0.5-1% | Investigar; si persiste → rollback |
| `CATALOG_VALIDATION_FAIL` por hora | > 5 | 2-5 | Revisar prompt / PCC; si sube → rollback |
| `DUAL FAILOVER` (Qwen→Gemini) por hora | > 10 | 5-10 | Revisar estabilidad DashScope / payload |
| `[TOOL-SUPPRESS] retry_failed fail_open` por hora | > 5 | 2-5 | Revisar guard numérico; considerar ajuste |
| Latencia p95 del webhook | > 15s | 10-15s | Revisar ruta Qwen vs Gemini |
| Mix de tráfico esperado | Qwen < 90% | Qwen 90-95% | Verificar flag/cache |
| Langfuse traces missing / exporter errors | > 0 | — | Telemetría rota; no avanzar a prod |

## Comandos de consulta rápida

### 1. Verificar ruta activa (últimos 10 min)
```bash
gcloud logging read "resource.type=cloud_run_revision AND resource.labels.service_name=bot-tiendalasmotos-beta" \
  --freshness=10m --limit=100 --format='value(textPayload)' --project=tiendalasmotos \
  | grep -E "QWEN ROUTE DECISION|QWEN ROUTE provider" | tail -20
```

### 2. Contar fallos PCC / failover (última hora)
```bash
gcloud logging read "resource.type=cloud_run_revision AND resource.labels.service_name=bot-tiendalasmotos-beta" \
  --freshness=1h --format='value(textPayload)' --project=tiendalasmotos \
  | grep -cE "CATALOG_VALIDATION_FAIL|DUAL FAILOVER|QWEN FORENSIC"
```

### 3. Verificar trazas Langfuse recientes
```bash
curl -s "https://cloud.langfuse.com/api/public/traces?limit=20" \
  -H "Authorization: Basic $(printf 'pk-lf-c43ffd34-ed29-4ed4-8054-1c772f319349:sk-lf-19b1662a-c29c-44d1-ab64-a251598fa2c6' | base64)" \
  | python3 -c "import sys,json; d=json.load(sys.stdin); print('traces', len(d.get('data',[]))); [print(t['timestamp'], t.get('userId')) for t in d.get('data',[])[:5]]"
```

### 4. Rollback manual inmediato (≤30s)
```python
from google.cloud import firestore
from datetime import timezone, datetime
db = firestore.Client(project="tiendalasmotos")
db.collection("llm_runtime").document("global").update({
    "qwen_enabled": False,
    "updated_at": datetime.now(timezone.utc).isoformat()
})
```
Confirmar en logs: `QWEN ROUTE DECISION] qwen_enabled=False` dentro de 30s.

## Checklist de la ventana F4.5
- [ ] 0h-6h: validar que >95% de las respuestas PASO 1 sean verdes (`has_price=True has_image=True has_ficha=True`).
- [ ] 6h-12h: revisar Langfuse por picos de latencia o errores de exportación.
- [ ] 12h-24h: ejecutar drill de rollback (false→Gemini) y re-flip (true→Qwen) documentando tiempos.
- [ ] 24h-48h: mantener observación; si todas las métricas están verdes, autorizar tráfico prod 10%→50%→100%.

## Criterio de salida
- **VERDE:** 24-48h sin rebasar umbrales ROJOS; drill de rollback < 30s; suite local sigue en 918/918.
- **ROJO:** Cualquier umbral ROJO persistente por >1h → rollback inmediato a Gemini y STOP; no avanzar a prod.

## Contactos / Owners
- **H14 pendiente:** registrar notificación a owners de dashboards Langfuse (quienes gestionan el proyecto `cmp0kl38703jvad07c1yq5xum`).
