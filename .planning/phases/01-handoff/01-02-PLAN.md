---
phase: 1
plan: 2
name: WhatsApp Router Refactor
wave: 2
depends_on: [01-01]
files_modified: [app/routers/whatsapp.py]
requirements: [R2]
---

# Plan 1-2: WhatsApp Router Refactor

## Objective
Refactorizar `app/routers/whatsapp.py` para leer `current_agent` desde la memoria del prospecto e invocar dinámicamente al servicio de Triaje (`triage_agent`) o al `CerebroIA` (`ai_brain`).

## Code Patterns (Model Resilience)
Reference these existing files for code style:
- `app/routers/whatsapp.py` — Error handling con except genérico y logueo, uso de dependencias globales.

Conventions:
- File naming: snake_case
- Error handling: Non-blocking, uso de `logger.exception`.
- Handoff atómico: Bloqueo de concurrencia no requerido, el estado reside en Firestore.

## Tasks

<task type="auto">
  <name>Importar TriageAgent provisional</name>
  <files>app/routers/whatsapp.py</files>
  <action>
    1. Importar el módulo `triage_agent` (aunque se creará en la Fase 2, se puede dejar el placeholder de la inyección o importar si ya se ha diseñado el stub). Como `triage_agent.py` no existe, primero dejaremos comentado el import o prepararemos la estructura if/else.
    2. Mejor aún: En esta fase solo preparamos la lógica de bifurcación usando un mock log o un stub si `triage_agent` no está inicializado.
  </action>
  <verify>python -m py_compile app/routers/whatsapp.py</verify>
  <done>La sintaxis es válida y la bifurcación está planeada.</done>
</task>

<task type="auto">
  <name>Implementar lógica de ruteo en _is_valid_message</name>
  <files>app/routers/whatsapp.py</files>
  <action>
    1. Modificar `_is_valid_message` para incluir la llamada a `memory_service.get_prospect_data(phone_number)`.
    2. Extraer `current_agent = prospect_data.get("current_agent", "triage")`.
    3. Bifurcar la lógica de procesamiento:
       - Si `current_agent == "triage"`, enrutar la petición al handler de Triaje (que se inyectará en Fase 2).
       - Si `current_agent == "finance"`, mantener el flujo actual hacia `ai_brain.process_message`.
  </action>
  <verify>python -m py_compile app/routers/whatsapp.py</verify>
  <done>El loop principal lee current_agent y bifurca.</done>
</task>

## Must-Haves
- `whatsapp.py` debe ser capaz de determinar quién es el "dueño" de la sesión actual sin causar fallos cíclicos.
- La lógica del `CerebroIA` se mantiene intacta bajo la rama `finance`.

---
*Created: 2026-05-05*
