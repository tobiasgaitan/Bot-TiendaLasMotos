---
phase: 1
plan: 1
name: MemoryService Extension
wave: 1
depends_on: []
files_modified: [app/services/memory_service.py]
requirements: [R1]
---

# Plan 1-1: MemoryService Extension

## Objective
Extender el `MemoryService` para persistir y recuperar el estado `current_agent` del prospecto, garantizando el handoff atómico entre el Triaje y el CerebroIA.

## Code Patterns (Model Resilience)
Reference these existing files for code style:
- `app/services/memory_service.py` — Native Async implementation (v6.9.6) and Non-Destructive Merge Strategy.

Conventions:
- File naming: snake_case
- Error handling: Use existing `logger.error` with `exc_info=True`.
- Types: Use `typing.Dict`, `typing.Any`.

## Tasks

<task type="auto">
  <name>Extender get_prospect_data</name>
  <files>app/services/memory_service.py</files>
  <action>
    1. En la función `get_prospect_data`, extraer `current_agent` de `data`.
    2. Agregar `current_agent` al diccionario de retorno. Si no existe en Firestore, el valor por defecto debe ser "triage".
    3. Asegurar que al devolver los datos por defecto cuando no hay `doc_ref`, también se incluya `"current_agent": "triage"`.
  </action>
  <verify>python -c "from app.services.memory_service import MemoryService"</verify>
  <done>El método retorna el agente actual o asume 'triage' por defecto.</done>
</task>

<task type="auto">
  <name>Extender create_prospect_if_missing</name>
  <files>app/services/memory_service.py</files>
  <action>
    1. En `create_prospect_if_missing`, inicializar el campo `current_agent` en "triage" dentro del diccionario `new_data`.
  </action>
  <verify>python -c "from app.services.memory_service import MemoryService"</verify>
  <done>Nuevos prospectos siempre nacen con current_agent='triage'.</done>
</task>

<task type="auto">
  <name>Agregar método update_current_agent</name>
  <files>app/services/memory_service.py</files>
  <action>
    1. Crear un nuevo método asíncrono `update_current_agent(self, phone_number: str, agent_name: str) -> bool`.
    2. Usar `_find_prospect_ref` para obtener la referencia.
    3. Si existe, ejecutar `await doc_ref.update({"current_agent": agent_name, "updated_at": firestore.SERVER_TIMESTAMP})`.
    4. Manejar excepciones con `logger.error` retornando False en caso de error, True en caso de éxito.
  </action>
  <verify>python -c "from app.services.memory_service import MemoryService"</verify>
  <done>Existe una vía atómica para cambiar el agente en control de la sesión.</done>
</task>

## Must-Haves
- El campo `current_agent` se expone en la carga de memoria.
- Por defecto, cualquier sesión sin el campo asume "triage".

---
*Created: 2026-05-05*
