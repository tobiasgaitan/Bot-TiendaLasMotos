---
phase: 2
plan: 1
name: Acoplar SemanticCacheService en CatalogService y Pruebas
wave: 1
depends_on: [01-01-PLAN.md]
files_modified: [app/services/catalog_service.py, tests/test_semantic_cache.py]
requirements: [R2, R4, R5]
---

# Plan 2-1: Acoplar SemanticCacheService en CatalogService y Pruebas

## Objective
Interceptar las consultas dentro de `CatalogService.search_items` de forma quirúrgica, validar el funcionamiento contra la caché (evitando latencia) y crear un test unitario que verifique el Price Consistency Check y similitud.

## Code Patterns (Model Resilience)
Reference these existing files for code style:
- `app/services/catalog_service.py` — Inyección en el Singleton `catalog_service` y en `__init__`.
- `tests/test_reset_flow.py` — Para el estilo de pruebas con `pytest`.

Conventions:
- Asegurarse de retornar el mismo formato de `unique_results[:3]` que retorna `search_items` actualmente.

## Tasks

<task type="auto">
  <name>Interceptar búsqueda en CatalogService</name>
  <files>app/services/catalog_service.py</files>
  <action>
    1. Importar `SemanticCacheService` y crear una instancia en el constructor `__init__` de `CatalogService`.
    2. En `search_catalog`, consultar:
       `cached, score = self._cache_service.get(query)`
    3. Si `cached` existe y `score > 0.85`:
       Hacer un `logger.info(f"⚡ Semantic Cache Hit (score: {score:.2f})")` y hacer `return cached` (que ahora será el bloque visual Markdown).
    4. Si no es hit, ejecutar la búsqueda normal, construir el texto Markdown final con la imagen y el precio, y guardarlo en caché:
       `self._cache_service.set(query, markdown_result)`.
    5. Adaptar `ai_brain.py` u otra interfaz para que soporte recibir la respuesta en formato Markdown en lugar del `list[dict]` crudo, protegiendo el PCC.
  </action>
  <verify>python -c "from app.services.catalog_service import CatalogService"</verify>
  <done>El código de CatalogService permite usar la caché semántica conservando el Price Consistency Check.</done>
</task>

<task type="auto">
  <name>Cobertura de Tests Unitarios</name>
  <files>tests/test_semantic_cache.py</files>
  <action>
    1. Crear `tests/test_semantic_cache.py`.
    2. Importar `pytest` y `SemanticCacheService`.
    3. Escribir un test que cree una instancia, inserte `"TVS Sport 100"` con una respuesta mock.
    4. Consultar con `"tvs sport 100"` y `"TVS Sport"` y confirmar que el `score > 0.85` y la caché es hit.
    5. Escribir un test para `CatalogService.search_items` (puede ser mockeando `_cache_service.get`) asegurando que el PCC se respeta.
  </action>
  <verify>pytest tests/test_semantic_cache.py</verify>
  <done>Los tests pasan y certifican el hit con variaciones tipográficas.</done>
</task>

## Must-Haves
- `npx agent-cli eval` ejecutado tras construir esto.

---
*Created: 2026-05-16*
