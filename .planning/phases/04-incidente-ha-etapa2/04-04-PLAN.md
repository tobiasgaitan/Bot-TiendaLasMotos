---
phase: 04
plan: 04-04
name: Validadores Regex PCC Pro + Sanitize PII
wave: 4
depends_on: [04-03b]
files_modified: [tests/validators.py, tests/test_pcc_ficha_tecnica.py, tests/test_pii_high_fidelity.py]
requirements: [HA-4]
---

# Plan 04-04: Validadores Regex PCC Pro + Sanitize PII

## Objective
Instrumentar validadores Regex reutilizables para (a) PCC Pro — Price Consistency Check: el precio mostrado al usuario debe coincidir EXACTAMENTE con el precio canónico del catálogo y la 'Ficha Tecnica:' debe ser explícita y no vacía; y (b) Sanitize PII — las salidas sanitizadas no deben filtrar PII cruda (teléfono CO, cédula, email) y `_sanitize_fields` debe eliminar control-chars. Cada validador se entrega con mutation check (debe FALLAR ante fixture mutado).

## Code Patterns (Model Resilience)
- `tests/test_pcc_ficha_tecnica.py` L1-60 — patrón existente: `re.search(r"Ficha Tecnica:\s*(.+)", res)` + patch.object(catalog_service, '_items', ...)
- `tests/test_pii_high_fidelity.py` — estilo unittest.TestCase sobre `clean_json_voorhees`
- `tests/factories.py` (04-02) — ítems dinámicos; PROHIBIDO reintroducir literales de precio

## Tasks

<task type="auto">
  <name>T1 — tests/validators.py</name>
  <files>tests/validators.py</files>
  <action>
    1. Constantes compiladas:
       - `RE_PRECIO_COP = re.compile(r"^\$\s?\d{1,3}(\.\d{3})+$")` (formato canónico "$ 6.200.000")
       - `RE_EXTRACT_PRECIOS = re.compile(r"\$\s?([\d\.]+)")` (extracción para comparación)
       - `RE_FICHA = re.compile(r"Ficha Tecnica:\s*(.+)")`
       - `RE_IMG_URL = re.compile(r"!\[.*?\]\((https?://[^)\s]+)\)")` (markdown imagen) y/o URL plana https
       - `RE_PHONE_CO = re.compile(r"(\+?57)?[\s-]?3\d{2}[\s-]?\d{3}[\s-]?\d{4}")`
       - `RE_CEDULA = re.compile(r"\b\d{6,10}\b")` (uso acotado a campos de documento)
       - `RE_EMAIL = re.compile(r"[A-Za-z0-9._%+-]+@[A-Za-z0-9.-]+\.[A-Za-z]{2,}")`
       - `RE_CONTROL_CHARS = re.compile(r"[\x00-\x08\x0b-\x0c\x0e-\x1f\x7f]")`
    2. Funciones:
       - `assert_price_consistency(response_text: str, expected_price_cop: int)`: extrae precios del texto, formatea esperado como `$ {expected:,}`→ separador '.', exige presencia exacta; AssertionError con ambos valores si difieren.
       - `assert_ficha_explicit(response_text: str)`: exige match de RE_FICHA con contenido no vacío ni "None".
       - `assert_no_pii_leak(text: str, allowed: tuple = ())`: exige 0 matches de phone/email (y cédula solo en campos documento si allowed lo indica).
       - `assert_no_control_chars(text: str)`.
    3. Cada función con docstring WHY (HA-4) y mensaje de AssertionError forense.
  </action>
  <verify>`./.venv/bin/python -c "from tests.validators import assert_price_consistency as a; a('Vale $ 5.000.000 Ficha Tecnica: X', 5000000)"`</verify>
  <done>Módulo de validadores importable y funcional</done>
</task>

<task type="auto">
  <name>T2 — Integración PCC Pro en test_pcc_ficha_tecnica.py</name>
  <files>tests/test_pcc_ficha_tecnica.py</files>
  <action>
    1. Nuevo test `test_pcc_price_consistency_regex`: catálogo dinámico (factories), `search_catalog` del ítem 0, `assert_price_consistency(res, items[0]["price"])` + `assert_ficha_explicit(res)`.
    2. Nuevo test `test_pcc_image_format_regex`: la respuesta contiene URL de imagen válida (RE_IMG_URL o URL plana https del ítem).
    3. Mutation check inline: mismo flujo con precio mutado (+10_000) → `pytest.raises(AssertionError)`.
    4. Migrar las aserciones regex inline existentes (L40-44) a `assert_ficha_explicit` (sin cambiar semántica).
    5. NO tocar los 2000+ tests financieros del archivo fuera de estos añadidos/migración localizada.
  </action>
  <verify>`./.venv/bin/pytest tests/test_pcc_ficha_tecnica.py -q` verde completo</verify>
  <done>PCC Pro instrumentado con validadores regex + mutation check</done>
</task>

<task type="auto">
  <name>T3 — Integración Sanitize PII en test_pii_high_fidelity.py</name>
  <files>tests/test_pii_high_fidelity.py</files>
  <action>
    1. Nuevo test `test_pii_no_leak_in_sanitized_output`: payload con phone CO (`+57 3XX XXX XXXX`), email y control-chars embebidos en campos de texto → tras `clean_json_voorhees`/`_sanitize_fields`, `assert_no_pii_leak` + `assert_no_control_chars` sobre los campos sanitizados.
    2. Test parametrizado de control-chars: cada char de RE_CONTROL_CHARS inyectado es eliminado.
    3. Mutation check: desactivar sanitización vía patch que retorna input crudo → `pytest.raises(AssertionError)` al aplicar assert_no_control_chars.
    4. Conservar el test de esquema existente intacto.
  </action>
  <verify>`./.venv/bin/pytest tests/test_pii_high_fidelity.py -q` verde completo</verify>
  <done>Sanitize PII instrumentado con validadores regex + mutation check</done>
</task>

<task type="auto">
  <name>T4 — Suite completa</name>
  <files>—</files>
  <action>1. `./.venv/bin/pytest -q` verde. 2. Conteo documentado en SUMMARY.</action>
  <verify>Salida pytest real</verify>
  <done>Wave 4 cerrada sin regresiones</done>
</task>

## Must-Haves
- [ ] tests/validators.py con 4 validadores regex documentados
- [ ] PCC Pro: consistencia exacta precio + ficha explícita + imagen, con mutation check
- [ ] Sanitize PII: no-leak phone/email + control-chars, con mutation check
- [ ] Suite completa verde

---
*Created: 2026-07-22 | Incidente H-A · BOT-BUILD-INCIDENT-HA-201*
