---
phase: 04
plan: 04-01
name: Rotación T0 + Reescritura Forense Git
wave: 1
depends_on: []
files_modified: [.planning/phases/04-incidente-ha-etapa2/]
requirements: [HA-1]
---

# Plan 04-01: Rotación T0 + Reescritura Forense Git

## Objective
Eliminar del árbol completo de Git (1100 commits, 7 ramas remotas, tags v1.0.1/v1.0.2) las credenciales filtradas del Incidente H-A: token Meta `EAA…{195-200}` (commits 1d681aa→f37ea28) y `webhookSecret` de whap.json (commits 2b200b1, 8b75d54). Precondición MANUAL y BLOQUEANTE: rotación de ambas credenciales (repo público — reescritura sin rotación es cosmética).

## Evidencia forense (verificada 2026-07-22)
- Token Meta: `git grep -E "EAA[A-Za-z0-9_-]{20,}"` en historial → `.github/workflows/qa-pipeline.yml` (len=200) y `tests/test_startup_lock.py` (len=195 ×2, variantes con `\n…\r`). Introducido en `1d681aa` (quick-049); tips actuales limpios desde era `dc5b106`.
- whap.json: `webhookSecret` (38 chars) + `webhookUrls` en `2b200b1` (creación) y `8b75d54` (modificación).
- NO filtrados: `key.json` (GCP SA, solo disco), `.env` (nunca commiteado), `.npmrc` (`_authToken` es ref `${ENV_VAR}`, len=18 — no literal).
- Fixture sintético allowlisted: `EAA_DUMMY_TOKEN_TEST` (17 chars tras EAA — NO debe tocarse; regla gitleaks lo exime por <20).

## Tasks

<task type="manual">
  <name>T0 — Rotación de credenciales (BLOQUEANTE)</name>
  <files>.planning/phases/04-incidente-ha-etapa2/04-01-ROTATION-CHECK.md</files>
  <action>
    1. Crear checklist de rotación con pasos: (a) Meta for Business / WhatsApp Business → revocar token EAATOsIH213… y emitir nuevo; (b) actualizar secreto WHATSAPP_TOKEN en GitHub Actions + GCP Secret Manager; (c) rotar webhookSecret de whap; (d) verificar deploy beta saludable post-rotación.
    2. El checklist queda PENDING hasta que el usuario lo confirme firmado.
    3. NINGÚN force-push se ejecuta antes de esta confirmación.
  </action>
  <verify>Archivo existe con checklist completo en estado PENDING</verify>
  <done>Checklist creado; ejecución automatizada puede continuar hasta pre-push</done>
</task>

<task type="auto">
  <name>T1 — Extracción de secretos a secrets.txt</name>
  <files>$TMPDIR/incident-ha-201/secrets.txt (NO se commitean valores; solo manifiesto redactado)</files>
  <action>
    1. `git grep -h -o -E "EAA[A-Za-z0-9_-]{20,}" $(git rev-list --all) | sort -u` → archivo (SIN cat/echo a stdout).
    2. Excluir `EAA_DUMMY_TOKEN_TEST` si apareciera (allowlist gitleaks).
    3. Extraer `webhookSecret` de `git show 8b75d54:whap.json` vía python json → añadir línea literal al archivo.
    4. Contar líneas (esperado: 2-5) y registrar SOLO longitudes+prefijos de 6 chars en el manifiesto redactado.
    5. NUNCA imprimir valores en logs/stdout/conversación.
  </action>
  <verify>`wc -l secrets.txt` ≥ 2 y manifiesto redactado generado</verify>
  <done>secrets.txt contiene todos los literales únicos; cero eco de valores</done>
</task>

<task type="auto">
  <name>T2 — Mirror clone + filter-repo</name>
  <files>$TMPDIR/incident-ha-201/Bot-TiendaLasMotos.git (espejo); .planning/phases/04-incidente-ha-etapa2/evidence/filter-repo.log</files>
  <action>
    1. `git clone --mirror origin $TMPDIR/incident-ha-201/Bot-TiendaLasMotos.git` (espejo fresco — el working tree sucio NO se toca).
    2. `git -C <espejo> filter-repo --replace-text secrets.txt --force` (replacement por defecto `***REMOVED***`).
    3. Capturar salida completa en filter-repo.log (contiene SHAs/conteos, NO secretos).
    4. Registrar conteo de commits reescritos y refs actualizados.
  </action>
  <verify>filter-repo.log existe y reporta "Repacking" / "New history written" sin errores</verify>
  <done>Historia del espejo reescrita en todos los refs+tags</done>
</task>

<task type="auto">
  <name>T3 — Verificación forense local del espejo</name>
  <files>.planning/phases/04-incidente-ha-etapa2/evidence/post-rewrite-scan.log</files>
  <action>
    1. En el espejo: `git grep -E "EAA[A-Za-z0-9_-]{20,}" $(git rev-list --all)` → DEBE ser 0 hits (o únicamente EAA_DUMMY_TOKEN_TEST si tuviera 20+ — no es el caso).
    2. `git log -p --all -- whap.json | grep -c '"webhookSecret": "\*\*\*REMOVED\*\*\*"'` ≥ 2.
    3. Re-escanear patrones hermanos: `BEGIN PRIVATE KEY`, `npm_[A-Za-z0-9]{36}`, `_authToken=[A-Za-z0-9]` → 0 hits.
    4. Verificar integridad: `git fsck --no-dangling` sin errores nuevos; `git rev-list --all --count` == 1100 (± nuevos commits de docs).
    5. Guardar salidas (redactadas) en post-rewrite-scan.log.
  </action>
  <verify>post-rewrite-scan.log muestra 0 hits de credenciales</verify>
  <done>Criterio de aceptación #1 demostrado EN EL ESPEJO, antes de cualquier push</done>
</task>

<task type="manual">
  <name>GATE — Confirmación de rotación + force-push</name>
  <files>origin (todos los refs)</files>
  <action>
    1. PREGUNTAR al usuario: "¿Rotación T0 confirmada?" — NO proceder sin sí explícito.
    2. `git -C <espejo> push --force --all origin && git -C <espejo> push --force --tags origin`.
    3. Realinear working repo: `git fetch origin` + `git reset --hard origin/beta` SOLO tras salvaguardar trabajo Etapa-1 en curso (commit previo en rama de rescate o stash confirmado con el usuario).
    4. Documentar en SUMMARY que SHAs antiguos persisten en caché de GitHub hasta GC; recomendar ticket a GitHub Support (repo público) — acción manual fuera de alcance.
  </action>
  <verify>`git ls-remote origin` muestra nuevos SHAs en todas las ramas/tags</verify>
  <done>Remoto saneado; working repo realineado; evidencia registrada</done>
</task>

## Must-Haves
- [ ] 04-01-ROTATION-CHECK.md existe y su estado es documentado (PENDING → CONFIRMED por usuario)
- [ ] `git grep` sobre `rev-list --all` del espejo = 0 hits de credenciales (acceptance #1)
- [ ] filter-repo.log + post-rewrite-scan.log en evidence/
- [ ] NINGÚN valor de secreto aparece en artefactos commiteados (solo prefijos/longitudes)
- [ ] Force-push ejecutado SOLO tras confirmación explícita de rotación

---
*Created: 2026-07-22 | Incidente H-A · BOT-BUILD-INCIDENT-HA-201*
