---
phase: 04
plan: 04-05
name: Certificación, Autopsia pytest y Cierre GSD
wave: 5
depends_on: [04-04]
files_modified: [.planning/phases/04-incidente-ha-etapa2/, .planning/STATE.md, .planning/ROADMAP.md, .planning/REQUIREMENTS.md]
requirements: [HA-1, HA-2, HA-3, HA-4]
---

# Plan 04-05: Certificación, Autopsia pytest y Cierre GSD

## Objective
Producir la evidencia final de los 4 criterios de aceptación del Incidente H-A: historial limpio (git grep rev-list --all = 0 hits), seam erradicado (rg = 0 hits), Coherence ≥ 0.9, 368/368+ tests verdes sin RuntimeWarning transversal; entregar la AUTOPSIA pytest como artefacto y cerrar documentación GSD.

## Tasks

<task type="auto">
  <name>T1 — Autopsia pytest (artefacto entregable)</name>
  <files>.planning/phases/04-incidente-ha-etapa2/PYTEST-AUTOPSY.md, .planning/phases/04-incidente-ha-etapa2/evidence/pytest-full-output.txt</files>
  <action>
    1. `./.venv/bin/pytest -v --tb=short --durations=20 -W error::RuntimeWarning 2>&1 | tee evidence/pytest-full-output.txt` (o sin `-W error` si RompeWarnings legítimos — documentar decisión; el criterio es CERO RuntimeWarning transversal, verificar con grep).
    2. PYTEST-AUTOPSY.md: conteo passed/failed/skipped, durations-20, sección de warnings (grep -i runtimewarning → vacío), lista de tests del guard migrados (04-02/03b) y validadores nuevos (04-04), hash del commit certificado, entorno (.venv Python 3.13).
    3. 0 fallos; cualquier skipped queda justificado línea a línea.
  </action>
  <verify>PYTEST-AUTOPSY.md existe con salida real completa</verify>
  <done>Autopsia entregada (acceptance #4)</done>
</task>

<task type="auto">
  <name>T2 — Coherence Score</name>
  <files>.planning/phases/04-incidente-ha-etapa2/evidence/agent-cli-eval.txt</files>
  <action>
    1. `npx agent-cli eval 2>&1 | tee evidence/agent-cli-eval.txt`.
    2. Extraer Coherence Score; DEBE ser ≥ 0.9. Pegar línea real en PYTEST-AUTOPSY.md y SUMMARY.
  </action>
  <verify>agent-cli-eval.txt muestra Coherence ≥ 0.9</verify>
  <done>Acceptance #3 certificado</done>
</task>

<task type="auto">
  <name>T3 — Re-verificación forense remota</name>
  <files>.planning/phases/04-incidente-ha-etapa2/evidence/final-forensic-scan.log</files>
  <action>
    1. Sobre el repo realineado post-push: `git fetch --all` y `git grep -E "EAA[A-Za-z0-9_-]{20,}" $(git rev-list --all)` → 0 hits.
    2. `git log -p --all -- whap.json | grep -c '\*\*\*REMOVED\*\*\*'` ≥ 2.
    3. `git ls-remote origin` — registrar SHAs finales de las 7 ramas + 2 tags.
    4. Documentar recordatorio: purga de caché GitHub (ticket Support) como acción manual pendiente fuera del repo.
  </action>
  <verify>final-forensic-scan.log con 0 hits</verify>
  <done>Acceptance #1 re-certificado sobre el remoto</done>
</task>

<task type="auto">
  <name>T4 — Cierre documental GSD</name>
  <files>.planning/STATE.md, .planning/ROADMAP.md, .planning/REQUIREMENTS.md, .planning/phases/04-incidente-ha-etapa2/04-VERIFICATION.md</files>
  <action>
    1. REQUIREMENTS.md: tabla V3 Incidente H-A (HA-1…HA-4) con estado Done y evidencias enlazadas.
    2. ROADMAP.md: entrada completada de Milestone 3 Etapa 2 con métricas (tests, coherence, refs reescritos).
    3. STATE.md: Current Position → Etapa 2 CERRADA; Coherence Score real; próximo hito.
    4. 04-VERIFICATION.md: tabla Must-Haves × evidencia de los 6 planes + cobertura HA-1…HA-4 + sección Gaps (o "None").
  </action>
  <verify>Archivos actualizados y commiteados (`docs(04): complete phase execution`)</verify>
  <done>Fase cerrada formalmente; listo para /gsd-verify (UAT conversacional)</done>
</task>

## Must-Haves
- [ ] PYTEST-AUTOPSY.md + pytest-full-output.txt (autopsia entregada)
- [ ] Coherence ≥ 0.9 evidenciado
- [ ] Escaneo forense final remoto = 0 hits
- [ ] STATE/ROADMAP/REQUIREMENTS/VERIFICATION actualizados y commiteados
- [ ] Tabla de los 4 acceptance criteria marcada con evidencia real

---
*Created: 2026-07-22 | Incidente H-A · BOT-BUILD-INCIDENT-HA-201*
