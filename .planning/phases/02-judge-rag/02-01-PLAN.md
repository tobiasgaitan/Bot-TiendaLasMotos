---
phase: 2
plan: 1
name: Security Closure and Fine-Tuning
wave: 1
depends_on: []
files_modified:
  - app/core/prompts.py
  - app/routers/whatsapp.py
requirements: [Tarea 2.3]
---

# Plan 02-01: Security Closure and Fine-Tuning

## Objective
Finalize the security and quality gate by synchronizing the AI personality with the Judge's criteria and ensuring an elegant fallback when the AI fails to meet quality standards.

## Code Patterns (Model Resilience)
Reference these existing files for code style:
- [app/routers/whatsapp.py](file:///Users/tobiasgaitangallego/Bot-TiendaLasMotos/app/routers/whatsapp.py) — Current retry loop and message handling logic.
- [app/services/judge_service.py](file:///Users/tobiasgaitangallego/Bot-TiendaLasMotos/app/services/judge_service.py) — 9 criteria definitions.

## Tasks

<task type="auto">
  <name>Synchronize System Prompt with 9 Criteria</name>
  <files>app/core/prompts.py</files>
  <action>
    Update `JUAN_PABLO_SYSTEM_INSTRUCTION` to include the 9 criteria of the Matrix (v9.8.0).
    Add sections for:
    - **Visual-Lock**: Explicitly mention that price and image are mandatory.
    - **Brilla Protocol**: Mention ID + 2 last gas receipts.
    - **City Discovery**: Mandatory check before credit advance.
    - **One-Question-Rule**: Enforce one question per message.
    - **Habeas Data**: Reiterate blocking profiling without consent.
  </action>
  <verify>cat app/core/prompts.py | grep "Visual-Lock"</verify>
  <done>Prompt reflects all 9 criteria.</done>
</task>

<task type="auto">
  <name>Implement Elegant Fallback and Observability (Text)</name>
  <files>app/routers/whatsapp.py</files>
  <action>
    Refactor the retry loop in `handle_message` (text branch):
    1. Update fallback message to: "Disculpa, no estoy seguro de la respuesta, permíteme le pregunto a mi supervisor y te comento."
    2. Add Langfuse observability:
       ```python
       if LANGFUSE_AVAILABLE:
           langfuse_context.update_current_trace(
               metadata={"last_rejection_reason": rejection_reason, "audit_attempts": attempts},
               tags=["JUDGE_CRITICAL_FALLBACK"]
           )
       ```
    3. Ensure the fallback message is saved to history as "model".
  </action>
  <verify>grep "JUDGE_CRITICAL_FALLBACK" app/routers/whatsapp.py</verify>
  <done>Retry loop has elegant fallback and observability.</done>
</task>

<task type="auto">
  <name>Integrate Audit Loop in Audio Path</name>
  <files>app/routers/whatsapp.py</files>
  <action>
    Copy the retry/audit logic from the text path to the audio path (`msg_type == "audio"`).
    Ensure the audio path also uses `JudgeService.analyze_response` with the same retry logic and fallback.
  </action>
  <verify>Check audit loop existence in the audio branch of handle_message.</verify>
  <done>Audio messages are audited by JudgeService.</done>
</task>

<task type="auto">
  <name>Final Verification and Audit</name>
  <files>app/routers/whatsapp.py</files>
  <action>
    Run a final audit to ensure no generic exceptions are caught without logging and all rules from the "user_global" are followed.
  </action>
  <verify>pytest tests/test_whatsapp_router.py (if exists) or manual verification logic.</verify>
  <done>System is stable and verified.</done>
</task>

## Must-Haves
- Fallback message is EXACTLY as requested.
- Langfuse tag is applied only on fallback.
- No "bot", "IA", or "technical error" mentioned in fallback.
