---
phase: 3
plan: 3
name: Gateway de Estado Transicional
wave: 2
depends_on: [03-01, 03-02]
files_modified: [app/routers/whatsapp.py]
requirements: [RF-2]
---

# Plan 3-3: Gateway de Estado Transicional (costura única)

## Objective
Unificar el guardado de estado transicional en una costura única dentro de `whatsapp.py`, mediante extracción estructural pura con cero cambio semántico (D2 del Plan Definitivo).

## Design
- `_open_session_and_refresh(ms, user_phone)` ← encapsula la secuencia L1200–1230: `create_prospect_if_missing` → `update_last_interaction` → `transition_to_in_progress` [ARCH-BULK-META-010, await bloqueante obligatorio] → re-fetch anti-stale [HOTFIX v9.8.3]. Devuelve `prospect_data` fresco. El orden queda pineado por CH-3.
- `_mark_ponytail_deprioritized(ms, user_phone)` ← reemplaza las 5 escrituras dispersas idénticas `update_prospect_summary(user_phone, "", {"ponytail_status": "DEPRIORITIZED"})` (L1414, L1454, L1652, L1690 y rama de audio) conservando payload, orden y correlación con `set_human_help_status(True)` (CH-4).
- Las escrituras PENDING/IN_PROGRESS/COMPLETED originadas en la extracción de `ai_brain.py` NO se tocan (lógica de negocio protegida — skill ponytail).

## Tasks

<task type="auto">
  <name>Extraer las dos funciones de costura y sustituir los call sites</name>
  <files>app/routers/whatsapp.py</files>
  <action>
    1. Extraer `_open_session_and_refresh` con el cuerpo exacto de la secuencia actual (incluidos logs).
    2. Extraer `_mark_ponytail_deprioritized` y reemplazar los 5 call sites verificando que cada uno mantiene su posición relativa frente a `set_human_help_status` y `save_message`.
    3. Ejecutar CH-3 y CH-4 más la suite contractual completa.
  </action>
  <verify>.venv/bin/python -m pytest tests/test_characterization_etapa1.py tests/test_ponytail_parametrization.py tests/test_agentic_loop_async.py tests/test_identity_legal_gate.py -v</verify>
  <done>CH-3/CH-4 verdes; 0 diffs de comportamiento; el impl reduce líneas sin tocar semántica.</done>
</task>

## Must-Haves
- Extracción carácter por carácter del cuerpo movido (integridad de bloques).
- Prohibido renombrar claves Firestore ni alterar payloads.
- CH-3 y CH-4 son la red: si fallan, revertir y reportar stack trace (prohibido rediseñar).

---
*Created: 2026-07-20 | Plan Definitivo D2*
