---
phase: 3
plan: 5
name: Higiene asíncrona y purga de maquinaria muerta
wave: 2
depends_on: [03-01, 03-02, 03-03]
files_modified: [app/routers/admin.py, app/services/audit_service.py, app/services/memory_service.py, app/main.py, app/routers/whatsapp.py]
requirements: [RF-4]
---

# Plan 3-5: Higiene asíncrona y purga de código muerto

## Objective
Resolver las contradicciones de asincronía detectadas en la auditoría PAA, alineando todo el repo con la doctrina Linear Blocking (BOT-INFRA-ASYNC-094). Cambios sin efecto observable.

## Design (D4 del Plan Definitivo)
1. `admin.py::refresh_config` L333: `config_loader.refresh()` → `await asyncio.to_thread(...)` (I/O síncrona de Firestore fuera del event loop).
2. `audit_service.py` L157: envolver el `create_task` huérfano en tarea rastreada con flush en shutdown, reutilizando el patrón `_pending_tasks` ya presente en MemoryService (reutilización, no abstracción nueva).
3. Cablear `MemoryService._track_task`/`shutdown()` (hoy 0 call sites) en el lifespan de `main.py` — el propósito arqueológico original: flush atómico de persistencia al apagar. Convertir código muerto en garantía real.
4. Purga: global redundante `motor_financiero` — OJO: NO es vestigial puro, se lee en `whatsapp.py` L1130 (`cerebro_ia.motor_financiero = motor_financiero`). Sustituir el global de módulo y su asignación en `_ensure_services_sync` (L242-246) por la referencia directa al singleton `financial_service` ya importado en el módulo; la asignación en L1130 debe apuntar al mismo objeto sin cambio observable. Deriva de versión en `memory_service.py` (docstring v9.6.0 vs logger v9.8.5 → unificar a la vigente).
5. Documentar como excepción conocida a CLI Readiness: I/O de red en import-time de `audit_service` (BigQuery setup al importar) — diferida a Etapa 2 (lazy-init con contrato nuevo).

## Tasks

<task type="auto">
  <name>Aplicar las 4 correcciones asíncronas y la purga</name>
  <files>app/routers/admin.py, app/services/audit_service.py, app/services/memory_service.py, app/main.py, app/routers/whatsapp.py</files>
  <action>
    1. to_thread en refresh_config.
    2. Tracking + flush en audit_service.
    3. Cablear shutdown() de MemoryService en el bloque de apagado del lifespan.
    4. Eliminar `motor_financiero` y unificar versión docstring.
    5. Anotar la excepción CLI Readiness en el plan de fase.
  </action>
  <verify>.venv/bin/python -m pytest tests/test_campaign_admin.py tests/test_health_check.py tests/test_startup_lock.py tests/test_chaos_firestore.py -v</verify>
  <done>Suites verdes; sin RuntimeWarning de corrutinas; refresh_config no bloquea el loop (test de latencia de eventloop si aplica).</done>
</task>

## Must-Haves
- Todo `except Exception` nuevo o tocado DEBE incluir `logger.exception` (Zero-Silent-Failures).
- El flush de shutdown NO puede elevar excepciones que impidan el cierre del proceso.

## Excepción conocida — CLI Readiness (Etapa 2)
`app/services/audit_service.py` ejecuta I/O de red en import-time (`audit_service = AuditService()` L185 instancia `bigquery.Client()` y provisiona dataset/tabla al importar el módulo). Se difiere a Etapa 2 (lazy-init con contrato nuevo). Mitigación aplicada en este plan: el flush de shutdown en `main.py` accede vía `sys.modules.get(...)` para no disparar dicho import durante el apagado.

## Addendum de ejecución [BOT-BUILD-REFACTOR-03-04-03-05]
- Ítem 3 reformulado: `MemoryService.shutdown()` YA estaba cableado en el lifespan (`main.py` L200); solo `_track_task` carecía de call sites. El patrón `_pending_tasks` se reutilizó en `audit_service`.
- Alcance ampliado (aprobado): `asyncio.to_thread` también en `reset_handoff` y `admin_health_check` (misma violación Linear Blocking no reportada por el Auditor).
- Purga `motor_financiero` ajustada: el nombre de módulo se CONSERVA (4 suites lo patchean) con binding eager al singleton `financial_service`; se elimina el estado lazy `None`.

## Addendum de ejecución [BOT-BUILD-REFACTOR-03-05-RESIDUAL]
- Brecha residual C-1/C-2 cerrada: `threading.RLock` interno + commit atómico assign-at-end en `ConfigLoader` (core, `load_all()`) y `FinanceConfigLoader` (`_refresh_cache()`). Getters jamás adquieren el lock (vía rápida intacta, INV-4).
- Autopsia pre-fix (`tests/test_refresh_atomicity.py`): 4.309.545/6.895.717 lecturas rasgadas (62,5%) en core; 1.871.206/4.365.607 (42,9%) en finance; default transitorio determinista `(2,None,1)` bajo inyección de fallo. Post-fix: **7/7 verdes**; fallback por documento preservado (INV-3, ponytail); suite completa **367/367**.
- Invariante de estrés formalizado: prohibidas secuencias decrecientes (lector adelantado al commit = tearing en vuelo) y prohibidos Nones (defaults transitorios); straddles no-decrecientes entre snapshots committed adyacentes son físicamente inevitables sin API `get_all()` y se aceptan documentadamente.
- Hallazgo PREEXISTENTE (reproducido con el diff retirado, fuera de alcance): envenenamiento del singleton `ConfigLoader` entre `test_startup_lock`/`test_config_startup` y `test_competitor_cache::test_hot_mutated_competitor_brands` bajo ciertos órdenes de ejecución parcial. No se manifiesta en la corrida completa de `tests/`.

---
*Created: 2026-07-20 | Plan Definitivo D4*
