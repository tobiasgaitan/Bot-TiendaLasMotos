---
phase: 3
plan: 1
name: Red de Caracterización (Feathers) del flujo Meta→Firestore
wave: 1
depends_on: []
files_modified: [tests/test_characterization_etapa1.py]
requirements: [RF-6]
---

# Plan 3-1: Red de Caracterización (Feathers) del flujo Meta→Firestore

## Objective
Instaurar la red de seguridad que captura el comportamiento ACTUAL del pipeline webhook (correcto o no) antes de cualquier fragmentación del enrutador, conforme al Algoritmo de Feathers (docs/OPENCODE.md Fase 7). Incluye el pin explícito del comportamiento defectuoso de doble procesamiento en `task_processor`, que el Plan 03-02 invertirá.

## Context
- Arquitectura aprobada: Plan Definitivo Etapa 1 [BOT-PLAN-REFACTOR-ETAPA1-197].
- Autopsia previa (WAVE1-199): `test_router_concurrency` y `test_webhook_sync_block` certifican la dedup SOLO en `webhook_handler` con `cloud_tasks_queue_path=None`; `task_processor` jamás recibe dos entregas del mismo wamid → falso positivo de idempotencia.

## Tasks

<task type="auto">
  <name>Crear tests/test_characterization_etapa1.py con 5 pins de comportamiento</name>
  <files>tests/test_characterization_etapa1.py</files>
  <action>
    1. CH-1: pin Feathers del comportamiento actual (defectuoso) — doble entrega del mismo payload a `task_processor` invoca `_handle_message_background` 2 veces. Comentario WHY: 03-02 invertirá esta aserción a 1.
    2. CH-2: la dedup de ingreso (`register_wamid` real) también gobierna el encolado Cloud Tasks — segundo webhook con mismo wamid retorna {"status": "ignored", "procesado": False} y `_enqueue_cloud_task` permanece en 1 llamada.
    3. CH-3: orden de la secuencia transicional en rama texto de `_handle_message_background_impl`: create_prospect_if_missing → update_last_interaction → transition_to_in_progress (índices relativos estrictos).
    4. CH-4: ante respuesta HANDOFF_TRIGGERED, `set_human_help_status(True)` se ejecuta ANTES de `update_prospect_summary(user_phone, "", {"ponytail_status": "DEPRIORITIZED"})` con payload exacto (invariante BOT-PONYTAIL-200).
    5. CH-5: egreso unificado — `save_message(phone, "model", response_text)` exactamente 1 vez y `_process_and_send_egress_message` invocado exactamente 1 vez (contrato BOT-BUGFIX-UNIFIED-EGRESS-PIPELINE-125).
  </action>
  <verify>.venv/bin/python -m pytest tests/test_characterization_etapa1.py -v</verify>
  <done>5/5 tests en verde sin tocar una sola línea de app/.</done>
</task>

## Must-Haves
- Prohibido modificar archivos bajo `app/` en este plan.
- Todo `except Exception` en código de test debe ser evitado; las aserciones son el canal de fallo.
- Reutilizar los patrones de mock probados de `tests/test_webhook_sync_block.py` (mínima expresión — skill ponytail).

---
*Created: 2026-07-20 | Ticket: BOT-BUILD-REFACTOR-ETAPA1-WAVE1-199*
