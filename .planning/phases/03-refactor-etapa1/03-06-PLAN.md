---
phase: 3
plan: 6
name: Extracción de pipelines del impl (Strangler Fig)
wave: 3
depends_on: [03-03, 03-05]
files_modified: [app/routers/whatsapp.py]
requirements: [RF-5]
---

# Plan 3-6: Extracción de pipelines de `_handle_message_background_impl`

## Objective
Fragmentar el núcleo transaccional (~1.116 líneas, L648–1764) en tres pipelines internos con dependencias inyectadas explícitamente, dejando el impl como orquestador lineal (<300 líneas objetivo). Strangler Fig: el router actual sigue operando mientras las costuras absorben la lógica. Firmas públicas (`webhook_handler`, `task_processor`, `_handle_message_background`) INMUTABLES.

## Design (D5 del Plan Definitivo)
1. **Pipeline de medios** (audio L700–1014, imagen/KYC L773–940): transcripción, visión, descarga.
2. **Pipeline cognitivo** (cerebro → `AgenticOrchestrator.run_checker` → `JudgeService` → fallback crítico L1400–1677).
3. **Pipeline de egreso** (unificado; ya semi-aislado en `_process_and_send_egress_message` L1771 + phase-gate image injection L1697–1740).

DI por parámetro desde servicios ya inicializados (`app.state`/globales del módulo); el LazyProxy se conserva como cargador. Mismo módulo salvo que el tamaño final exija submódulo (decisión del BUILDER justificada en el commit).

## Tasks

<task type="auto">
  <name>Extraer los tres pipelines y reducir el impl a orquestador</name>
  <files>app/routers/whatsapp.py</files>
  <action>
    1. Extraer pipeline de medios preservando orden de awaits y logs forenses.
    2. Extraer pipeline cognitivo (juez + fallback) usando el gateway de 03-03.
    3. Consolidar egreso sobre `_process_and_send_egress_message`.
    4. Verificar CH-5 (egreso unificado: 1 save model + 1 envío) y suite completa.
  </action>
  <verify>.venv/bin/python -m pytest tests/ -x --ignore=tests/integration --ignore=tests/performance -q</verify>
  <done>357+ tests verdes; impl <300 líneas; aristas `uses` del router hacia servicios reducidas (meta ≤6 vía DI).</done>
</task>

## Must-Haves
- Cero cambio semántico: orden de awaits, payloads Firestore, copys y scripts legales intactos (skill ponytail).
- Blindaje God Node: verificar vecinos del grafo antes de mover cada bloque (docs/OPENCODE.md Fase 3).

---
*Created: 2026-07-20 | Plan Definitivo D5*
