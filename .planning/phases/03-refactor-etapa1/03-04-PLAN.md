---
phase: 3
plan: 4
name: Desambiguación FinanceConfigLoader
wave: 2
depends_on: [03-01]
files_modified: [app/services/config_loader.py, app/main.py, scripts/verify_fase1.py, scripts/debug_config_loaders.py]
requirements: [RF-3]
---

# Plan 3-4: Desambiguación de namespaces ConfigLoader (R-C)

## Objective
Ejecutar la resolución R-C del Auditor: la clase `ConfigLoader` de `app/services/config_loader.py` (SSOT financiera "Fase 1", TTL 300s) pasa a llamarse **`FinanceConfigLoader`** y queda absorbida por el ecosistema financiero. El identificador `ConfigLoader` queda reservado exclusivamente a `app/core/config_loader.py` (matriz de personalidad + routing rules). Esto desconfla el God Node #1 del grafo (1.643 aristas medidas sobre dos clases homónimas fusionadas).

## Radio de explosión (medido en auditoría — 4 archivos)
1. `app/services/config_loader.py` — renombrar clase y ajustar docstrings internos.
2. `app/main.py` L59 — `LazyProxy("app.services.config_loader", "FinanceConfigLoader")`. El alias público `app.main.FinanceConfigLoader` PERMANECE IDÉNTICO ⇒ los 5 `patch("app.main.FinanceConfigLoader")` de `tests/test_startup_lock.py` siguen verdes sin tocarse.
3. `scripts/verify_fase1.py` L9 — actualizar import.
4. `scripts/debug_config_loaders.py` L13 — actualizar import (elimina alias manual redundante).

Boy Scout incluido: corregir docstring de `app/core/config_loader.py` L72 (`sebas_personality` → `juan_pablo_personality`). El renombrado del ARCHIVO físico se difiere explícitamente (la colisión dañina era de clase, no de path).

## Tasks

<task type="auto">
  <name>Renombrar la clase homónima y sus 4 call sites</name>
  <files>app/services/config_loader.py, app/main.py, scripts/verify_fase1.py, scripts/debug_config_loaders.py</files>
  <action>
    1. Renombrar `class ConfigLoader` → `class FinanceConfigLoader` en app/services/config_loader.py (incluye `_instance`/`__new__` singleton internos).
    2. Actualizar main.py L59, scripts/verify_fase1.py L9, scripts/debug_config_loaders.py L13/L31.
    3. Boy Scout: docstring L72 de app/core/config_loader.py.
    4. Verificar imports CLI: `python3 -c "from app.services.config_loader import FinanceConfigLoader"`.
  </action>
  <verify>.venv/bin/python -m pytest tests/test_startup_lock.py tests/test_config_startup.py -v && npx agent-cli scaffold --check</verify>
  <done>Suite startup verde; 0 referencias residuales a `app.services.config_loader.ConfigLoader`; grafo regenerado sin conflación (verificación diferida al cierre de fase).</done>
</task>

## Must-Haves
- NO tocar `app/core/config_loader.py` salvo el docstring Boy Scout.
- Comportamiento observable idéntico (singleton, TTL, defaults financieros).

## Addendum de cierre [BOT-BUILD-REFACTOR-03-05-RESIDUAL]
- Certificación diferida del grafo EJECUTADA (2026-07-21): **0 aristas cruzadas** core↔finance; `FinanceConfigLoader` aislado (22 aristas); corpus saneado vía cuarentena `attic/` + `.graphifyignore` (145 archivos escaneados); 0 nodos huérfanos (GC de 50 nodos con path inexistente).
- Métrica de éxito redefinida: el conteo absoluto de `ConfigLoader` (1.671 aristas, 97,8% `uses` inferidas de comentarios `# WHY` vivos en whatsapp/catalog_service/ai_brain) se documenta como artefacto **informativo** del corpus, no como defecto. La desconflación se mide por aristas cruzadas (=0), no por el ranking God Node.

---
*Created: 2026-07-20 | Resolución R-C del Auditor*
